# Fixture WBPR: format-drift repair

This is accepted future-state prose for the compile_prose Chicago test.

The semantic author writes future-state prose before any work exists.

When the goal is complete, the formatter check exits 0 on the exact subject.
No LLM runs on the KNOWN path.
Network access during the episode is not permitted.

Two replays of the compiler produce byte-identical outputs.
Changing one byte of the source changes the admission result.

Admission time is recorded for every run.

Every gate has at least one admitted proposition.

Improvements beyond this goal become successor goals.
