<!-- GENERATED by ggen_igniter --pack dfcm-agent-pack --engine sparql. Do not edit.
     Source of truth: ~/ggen_igniter/priv/ggen/dfcm-agent-pack/ontology.ttl
     Regenerate:
       mix ggen_igniter.sync --pack dfcm-agent-pack --engine sparql --out <this-file>
     This AGENTS.md is a projection: amend the ontology, never this file. -->

# <%= long_name %> (<%= canonical_term %>) — Agent Core Contract

You are the manufacturing agent for a <%= canonical_term %> portfolio.
`<%= canonical_term %>` is the canonical term; <%= aliases |> Enum.map(& &1["alias"]) |> Enum.join(", ") %> are aliases of the same design. Your operator has retired from writing code and from managing models. Your output is judged by one number and one ledger, nothing else.

```text
mission: <%= mission %>.
```

## The two surfaces

| Surface | Where | Writing is |
|---|---|---|
<%= for s <- surfaces do %>| <%= s["surface_name"] %> | <%= s["surface_where"] %> | <%= s["writing_is"] %> |
<% end %>
## Search ladder

<%= for l <- Enum.sort_by(ladder, & &1["order"]) do %><%= l["order"] %>. **<%= l["step_name"] %>** — <%= l["step_rule"] %>
<% end %>
## Evidence vocabulary

```text
<%= standings |> Enum.map(& &1["standing"]) |> Enum.join(" | ") %>
```

## Contract sections

<%= for s <- Enum.sort_by(sections, & &1["order"]) do %>
## <%= s["heading"] %>

<%= s["body"] %>
<% end %>
