# GALL Checkpoint 002 — Project Manufacturer Seal

Status: DRAFT IMPLEMENTATION CONTRACT
Repository: `seanchatmangpt/ggen_igniter`
Exact admitted base: `d84da1419a6945c6a8a64b8f6cdca9d0b2c9e0f3`
Branch: `gall/checkpoint-002-project-manufacturer`
Owner surface: admitted semantic input -> upstream Ash/Igniter generators -> projected application artifacts
Authority ceiling: `CONSTRUCT`

## Context

The previous v26.9.16 RFC closure PR was based on `15305cea...` and mostly documented the desired closure plus a CI cache repair. Current `main` has advanced through RFC-GPACK manifest/identity correspondence. This checkpoint starts from the live head and does not promote stale branch history as evidence.

The Ash doctrine remains binding: if Ash/Igniter already knows how to manufacture the thing, this repository MUST compose the upstream generator rather than hand-write its projection.

## Required transition

`admitted graph + bootstrap manifest -> manufacturer identity -> upstream generator composition -> generated projection set -> attestation -> replayable construction receipt`

MachineExperience compile-back, when present, enters through the same admitted manufacturer path. It receives no privileged generation shortcut.

## Required implementation

1. Require exact admitted semantic subject identity before project construction.
2. Bind the manufacturer identity to the exact generator implementation/configuration/profile used.
3. Bind the projection identity to the exact generated byte set.
4. Preserve and extend the existing post-run-hash attestation so attested bytes are exactly the receipted bytes.
5. Refuse projection drift: a hand edit to a generated artifact cannot silently preserve semantic standing.
6. Prove deterministic regeneration within the same admitted profile.
7. Ensure generated Ash resources/domains/config/migrations are created through real Ash/Igniter generators, not handwritten substitutes.
8. Accept compile-back inputs only after qualification/admission and route them through ordinary manufacture.
9. Emit construction evidence compatible with downstream `ash_a2a` semantic-subject/receipt binding.
10. Carry forward the useful CI-cache separation from the superseded closure line only if it remains necessary on this exact base.

## Required identity tuple

`SemanticSubject = {graph_digest, manifest_identity, manufacturer_digest, projection_digest, profile}`

Changing any member MUST either change the subject identity or produce a typed refusal. No cache hit, generated source edit, or previous receipt may erase this relation.

## GALL receipt fields

- repository + exact PR-head SHA;
- `mix.lock` digest;
- Elixir/OTP/Rebar/Rustler/Rust toolchain identities used by the court;
- admitted graph + `pack.toml` identities;
- manufacturer digest;
- upstream generator task identities;
- generated projection file set + byte digests;
- post-run hash;
- attestation digest;
- regeneration command/exits;
- drift/refusal witness;
- compile-back admission identity when exercised;
- predecessor GALL-001 receipt identity when a Rust ggen pack is the producer;
- standing.

## Falsifiers

- construct from an unknown/unadmitted semantic subject;
- change generator version/configuration while manufacturer identity remains unchanged;
- hand-edit a generated Ash projection and retain `ALIVE` without regeneration;
- alter one output between receipt and attestation and still attest it;
- generate Ash source without the real upstream generator-owned registration/config/snapshot mutations;
- allow generated code to grant itself authority;
- let MachineExperience compile-back bypass normal admission/manufacture;
- restore stale `_build` state that causes the exact subject to execute different native/generator machinery while preserving standing.

## Verification ladder

1. `mix format --check-formatted`
2. `mix compile --warnings-as-errors`
3. focused manifest/admission + ephemeral-attestation + manufacturer identity tests
4. focused real Ash manufacture fixture using the actual upstream generator composition path
5. deterministic regeneration/drift court
6. `mix test`
7. `mix e2e` when the claimed closure depends on a real scaffolded consumer project boundary

Hosted CI supplements these observations; it does not replace exact-subject execution evidence.

## Dependencies

Upstream: GALL-001 when consuming a ggen-produced pack/receipt.

Downstream:

- GALL-003 `ash_a2a` command subject binding;
- GALL-005 `autofde-lab` composition crown.

## Exclusions

- no authority grant;
- no external DO;
- no handwritten generated Ash implementation;
- no cross-repo crown;
- no release/publication/merge standing.

## Definition of done

At one exact PR head, a real admitted semantic subject manufactures a real Ash/Igniter projection through upstream generators, the output is content-addressed and attested, clean regeneration is deterministic, direct generated-source drift is refused, and compile-back cannot bypass the ordinary manufacturer path.

Standing on completion: `ALIVE` only for the exact project-manufacturer subject.