# 013 - Semantic Jira over ash_a2a (XaaS A2A)

Branch: `feat/semantic-jira-ash-a2a` (worktree `/Users/sac/ggen_igniter-wt/a2a`), base `feat/calver-ticket-day-pack` @ 5b30106.

## Charter

Semantic Jira work orders become A2A tasks; Semantic Jira actions become A2A
skills; the Ash/ash_a2a surface is generated from the ontology.

## Delivered

- Ontology: `a2a:` vocabulary, state map, skills, refusals, manufacture target
  (`priv/ggen/semantic-jira-pack/ontology.ttl`), SHACL shapes, gates 060-064.
- Templates: `a2a_manufacture.ex.eex`, `a2a_agent_card.json.eex`; projection
  type `a2a_agent_card` (15 projection types).
- `GgenIgniter.SemanticA2A` (pure Elixir): `state_for/3`, `task_from_work_order/2`,
  `receipt/3`, `verify_chain/1`, `wire_state/1`.
- gall pack: `gall:CodingCheckpoint` carries `sj:projection-a2a-card`.
- Tests: pack (21), manufacture (8), dispatch + HTTP (12), all Chicago, no doubles.

## Falsifiers

F1 ALIVE without receipt is never `completed`; F2 nil/ungranted `:change` does
not execute; F3 Push/Publish never on the card; F4 replay digest stable; F5 no
`use Ash.Resource` in the rendered task; F6 no function takes task state as
standing; F9 second manufacture run changes nothing.

## BLOCKED / UNSUPPORTED

- UNSUPPORTED(generator-capability): `external_do` skills.
- UNVERIFIED: no public RDF A2A ontology.
- Data layer is ETS, not Postgres.
