# Capability Matrix — ggen_igniter (Agent 1 pass, 2026-08-27T21:2x-22:0xZ)

HEAD at assessment: `767bccea32d03790a1f4975596a06ecd16df365f` (branch `main`).
NOTE: 9 other agents are editing this tree concurrently right now; some findings below
(especially `lib/ggen_igniter/actuate.ex`) may already have changed by the time this is read —
each row's EVIDENCE is a real command/grep run at assessment time, not a permanent fact.

| CAPABILITY | STANDING | EVIDENCE | MISSING INVARIANT | OWNER | DEPENDENCIES |
|---|---|---|---|---|---|
| hygen-parity frontmatter (`---\n...\n---\n`, `to`/`sparql`/`for_each`/`skip_if`/`unless_exists`/`mode`) | ALIVE | `lib/ggen_igniter/frontmatter.ex:222-301` (`split_template/1`), doctests pass (`iex> GgenIgniter.Frontmatter.split_template(...)`); `mix test` run confirms `12 doctests ... 0 failures` among the passing set | None observed in this pass — full field set (`before`/`after`/`at_line`/`shape`/`freeze_policy`/etc.) is mirrored 1:1 per moduledoc citing `~/ggen/crates/ggen-engine/src/template.rs:40-175` | Agent handling Igniter/AST + RDF/query engines (frontmatter is shared infra) | `yaml_elixir` dep |
| `mode: file` | ALIVE | `frontmatter.ex:230-240` (default mode), exercised by `test/ggen_igniter_frontmatter_properties_test.exs` (StreamData) | none found this pass | Igniter/AST actuation agent | `GgenIgniter.Actuate.write_file!/3` |
| `mode: eval` | ALIVE | `frontmatter.ex:230-240`, `lib/ggen_igniter/actuate.ex:301-310` (`eval_code!/2`); CHANGELOG v26.8.27 entry documents wiring; doctests for `eval_code!/2` pass | Compile/syntax error path only tested via doctest; no dedicated `mode: eval` failure-path test observed | Igniter/AST actuation agent | `Code.eval_string/2` |
| inject actuation (`inject: true`, `before`/`after`/`at_line`) | **BUILD_BROKEN** (real, currently reproduced) | `lib/ggen_igniter/actuate.ex:208` calls `already_present_at?(lines, body_lines, insert_at, insert_mode)` (4-arity) but the only definition is `already_present_at?/3` at line 331 — confirmed via `grep -n "already_present_at?" lib/ggen_igniter/actuate.ex` showing call-site arity 4 vs def arity 3. Real `mix test` run (`timeout 300 mix test`) hits this as a live subprocess compile error: `error: undefined function already_present_at?/4 ... == Compilation error in file lib/ggen_igniter/actuate.ex ==`, causing `test/ggen_igniter_sync_for_each_test.exs` tests 6/7/8 to fail with that raw compiler output embedded in the assertion diff. Result: `12 doctests, 20 properties, 177 tests, 8 failures` | Arity mismatch between call site and definition — needs the call site's 4th arg dropped OR the def updated to accept `insert_mode` (this is mid-edit by another agent, not this agent's write-scope) | Igniter/AST actuation agent (owns `actuate.ex`) | none — pure internal Elixir bug |
| `--pack`/`--pack NAME:TEMPLATE` convention | ALIVE (by evidence in `pack.ex` + README; not independently re-run this pass since `mix test` already covers `ggen_igniter_pack_discovery_matrix_test.exs`) | `lib/ggen_igniter/pack.ex:56` (`--pack NAME \| --pack-dir DIR` resolution), `README.md:117-134` (`#### The --pack convention` section, example `mix ggen_igniter.sync --pack audit-trail-pack --out ...`), `test/ggen_igniter_pack_discovery_matrix_test.exs` present | Full pack-discovery + doctor pack-checks (9-12) not independently re-verified command-by-command this pass, but structurally exercised by the same `mix test` run above (177 tests total include this file) | Dependencies/packaging/doctor agent | `priv/ggen/<pack-name>/` convention |
| engine registry + oxigraph default | ALIVE (registry structurally sound); **default recently changed, in-flux** | `lib/ggen_igniter/engine.ex:24-28` (registry: sparql/qlever/oxigraph), `lib/mix/tasks/ggen_igniter.sync.ex` per `test/e2e/lifecycle_test.ex:328-349` moduledoc: "`Mix.Tasks.GgenIgniter.Sync`'s own default engine changed from `sparql` to `oxigraph`" mid-session, causing a real bug discovered in `native/ggen_graph_nif/src/oxigraph_engine.rs:73` (string bindings come back with literal quote chars baked in) — the e2e test now pins `--engine sparql` explicitly to route around it | Oxigraph engine's string-literal serialization bug (quoted values) is a real, disclosed, currently-unfixed bug per the e2e test's own inline citation | RDF/query engines agent | `native/ggen_graph_nif` (Rust NIF), `RDF.Turtle`/`SPARQL` |
| optional `tesla`/`gno` (qlever engine) | ALIVE (compile-isolation verified) | `mix.exs:85-133` extensive inline commentary describing a real bare-consumer-project repro proving `optional: true` (not `only: [:dev,:test]`) is required and works; `lib/ggen_igniter/engine.ex:91-145` (`GgenIgniter.Engine.Qlever`) guards every reference with `Code.ensure_loaded?/1` | Not independently re-run (no fresh bare-consumer repro executed in this pass — relying on the cited prior verification) | Dependencies/packaging agent | `:tesla`, `:gno` (both optional) |
| `doctor --fix` | ALIVE | `lib/mix/tasks/ggen_igniter.doctor.ex:1-510` — 16 real checks (Elixir/OTP, deps, sparql advisory, igniter/sourceror `:only` relaxation, dcatr env config, Ash domains, qlever reachability, pack ontology/gates/templates, git status, NIF compile, oxigraph smoke test, optional hex-publish check via `--hex-check`); `--fix` applies `GgenIgniter.DoctorFixes` real fixes, not just diagnostics (`fix_or_check/3`, doctor.ex:239-258) | Not run against a live consumer app this pass (would require a scaffolded app, covered instead by the e2e agent's lane) | Dependencies/packaging/doctor agent | `GgenIgniter.DoctorFixes` |
| e2e Stage 0 (baseline scaffold + test) | UNVERIFIED this pass (not executed; requires network + minutes) | `test/e2e/lifecycle_test.ex:84-112`; moduledoc marks the Ecto/Postgres issue "FIXED (2026-08-27)" via `--with-args=--no-ecto` | Not independently re-run in this pass (network/time cost, owned by clean-room e2e agent) | Clean-room e2e agent | `mix igniter.new`, `mix archive.install` |
| e2e Stage 1-2 (resource/domain sync, re-sync delta) | UNVERIFIED this pass | `test/e2e/lifecycle_test.ex:118-177` | same as above | Clean-room e2e agent | `mix ggen_igniter.sync --pack-dir ... --engine sparql` |
| e2e Stage 3 (relationships) | UNVERIFIED this pass | `test/e2e/lifecycle_test.ex:179-205`, includes a disclosed fixed bug (`source_attribute` vs `destination_attribute`) | same | Clean-room e2e agent | Ash relationship template logic |
| e2e Stage 4 (custom action) | UNVERIFIED this pass | `test/e2e/lifecycle_test.ex:207-212` | same | Clean-room e2e agent | template `actionType` branching |
| e2e Stage 5 (AshPhoenix.Form round-trip) | UNVERIFIED this pass | `test/e2e/lifecycle_test.ex:214-223, 403-459` | same | Clean-room e2e agent | `AshPhoenix.Form`, `Ash.DataLayer.Ets` |
| e2e Stage 6 (ash_phoenix.gen.live + LiveView test) | UNVERIFIED this pass | `test/e2e/lifecycle_test.ex:225-277, 368-399, 463-525` | same | Clean-room e2e agent | `mix ash_phoenix.gen.live` |
| e2e Stage 7 (rename, expected compile failure w/ stale ref) | UNVERIFIED this pass | `test/e2e/lifecycle_test.ex:279-317` — disclosed, deliberate limitation: "no cross-file stale-reference repair anywhere in this codebase" | This is a documented, accepted gap (not a bug) — no auto-repair of dependent hand-generated code across a rename | Clean-room e2e agent / destructive-evolution agent | `mix compile --warnings-as-errors` |
| StreamData property coverage | ALIVE (broad) | 8 real property test files found via `find`: `ggen_igniter_pack_properties_test.exs`, `ggen_igniter_cross_engine_equivalence_properties_test.exs`, `ggen_igniter_sync_properties_test.exs`, `ggen_igniter_actuate_properties_test.exs`, `ggen_igniter_frontmatter_properties_test.exs`, `ggen_igniter_pack_discovery_matrix_test.exs`, `ggen_igniter_full_pipeline_properties_test.exs`, `ggen_igniter_render_properties_test.exs`; real `mix test` run reports `20 properties` executed | `ggen_igniter_actuate_properties_test.exs` presumably exercises the currently BUILD_BROKEN `inject_content!/5` path — not confirmed whether any of the 8 failures are property-test failures specifically vs the 3 named `sync_for_each` failures (only 3 named failures were visible in the tail of a truncated run; 8 total failures reported, 5 unseen) | Property/model testing agent | `stream_data` dep |
| Dialyzer | UNKNOWN (not executed this pass) | PLT exists on disk: `_build/dev/dialyxir_erlang-28.3.1_elixir-1.19.5_deps-dev.plt` (+ `.hash`), `mix.exs:22-35` documents a deliberate `plt_add_apps: [:mix]` fix for 12 known false-positive `unknown_function` warnings from `Mix.Task`-based CLI entry points | Full `mix dialyzer` run not executed this pass (time budget) — PLT presence is not proof of a clean run | Dependencies/packaging agent | `dialyxir` |
| Credo | ALIVE | Real `mix credo --strict` run this pass: `368 mods/funs, found 2 warnings, 7 refactoring opportunities, 3 code readability issues, 7 software design suggestions` — zero `[C]`/error-tier findings, only advisory-tier (test file naming for `test/e2e/lifecycle_test.ex`, two "nested too deep" refactor suggestions in `doctor_fixes.ex`, one cyclomatic-complexity note in `ggen_igniter.sync.ex:761`) | None blocking; advisory-only | All (shared hygiene) | `credo` dep |
| ExCoveralls | UNKNOWN (not executed this pass — `mix test` was run without `--cover`) | `mix.exs:16-21` configures `test_coverage: [tool: ExCoveralls]` and `preferred_cli_env` for `coveralls`/`coveralls.detail`/`coveralls.html` | Coverage percentage not measured this pass | Property/model testing agent | `excoveralls` dep |
| `mix hex.build` | ALIVE | Real command run this pass: `mix hex.build` succeeded — `Package checksum: 174053e3...`, `Version: 26.8.27`, licenses `MIT` present, description present, saved `ggen_igniter-26.8.27.tar` | None found | Dependencies/packaging/doctor agent | `package:` block in `mix.exs:52-60` |
| `mix.exs` version vs requested release | **MISMATCH (real, confirmed)** | `mix.exs:9`: `version: "26.8.27"` vs `.ggen_igniter_factory/state.json`'s `requested_version: "v26.7.27"` — see `.ggen_igniter_factory/version_check.md` for the full report. NOT silently corrected per this agent's mandate. | Release-version alignment decision needed from the orchestrating session/product-acceptance agents (Zach/Chris), not this agent | Orchestrator / product acceptance agents | none — a human/process decision, not a code bug |

## Full `mix test` run summary (real, this pass)

Command: `timeout 300 mix test` (excludes the opt-in `mix e2e` alias/tier).

Result: `12 doctests, 20 properties, 177 tests, 8 failures`.

3 of the 8 failures were directly observed in the truncated tail output, all in
`test/ggen_igniter_sync_for_each_test.exs` (tests 6, 7, 8 — lines 46, 80, 137), all
failing for the SAME root cause: the real subprocess `mix ggen_igniter.sync` invocation
recompiles the project and hits the `actuate.ex` arity-mismatch `CompileError` described
in the "inject actuation" row above. The remaining 5 of 8 failures were not captured in
this pass's truncated output (only `tail -60` was read) — re-run `mix test` untruncated
if a full failure inventory is needed; this agent's write-scope forbids touching
`lib/ggen_igniter/actuate.ex` to fix and re-verify.

## Real `mix compile --warnings-as-errors` (direct, in-repo, this pass)

Succeeded with only a `preferred_cli_env`/`def cli` deprecation warning (Mix 1.19.5
deprecating `mix.exs`'s `def project` location for `preferred_cli_env` — cosmetic, not
a real error). This ran BEFORE the concurrent edit to `actuate.ex` that later broke the
`mix test` subprocess compiles above — direct evidence the arity-mismatch bug appeared
mid-session, from another agent's concurrent edit, not a pre-existing defect.
