# HANDWRITTEN — the 1% ledger

Law: `priv/ggen/dfcm-agent-pack/ontology.ttl` (DfCM hand-writing gate). A hand-written
product artifact is admitted only when no admitted pack/generator expresses the semantic
element, an `UNSUPPORTED(generator, element)` row names the exact gap in the owning
ontology, and a row appears here. The ledger must shrink monotonically per milestone;
growth requires a paydown plan in the same change.

Format: `path | semantic element | missing capability | intended owner pack | date`

---

test/ggen_igniter_semantic_jira_pack_test.exs | Chicago-style, no-mock ExUnit integration proof of semantic-jira-pack's full pipeline (canonical WorkOrder graph -> oxigraph/sparql admission -> for-each Reactor render -> real filesystem actuation -> durable GgenIgniter receipts -> byte-identical replay; includes falsifier, DO fail-closed, and promotion-boundary proofs) | no admitted generator manufactures pack-integration proof tests — the only test-manufacturing families (beam4pm-bench-pack, chicago-fault-injection-pack) emit domain-specific benchmark/fault-injection specs, and pack gates verify admission data, not end-to-end pipeline behaviour | semantic-jira-pack: extend the test-manufacturing family with a pack-integration-proof generator (ontology facts + template over the pack's own manifest/gates); when admitted, this row and the `sj:ledger-unsupported-001` UNSUPPORTED row retire | 2026-09-19

test/ggen_igniter_semantic_jira_goal_checkpoint_test.exs, test/fixtures/semantic-jira-goal-checkpoint/goal.ttl | Chicago-style SHACL admission proof of the Friday GoalCheckpoint vocabulary (GC-FRI-0800 G4): tuple-complete goal graph admits, per-tuple-field mutation table refuses naming the field, root sj:stopQuery / non-root sj:courtCommand + sj:boundaryClass laws, legacy orders unaffected, MachineExperience shape vs machine_experience/1, jira projection of a Friday order | UNSUPPORTED(generator-capability): no admitted generator manufactures pack admission-proof tests or their fixture graphs (ontology row `sj:ledger-unsupported-002`) | semantic-jira-pack: the pack-integration-proof generator of `sj:ledger-unsupported-001` also emits one mutation case per `sh:minCount` / tuple `sh:sparql` constraint read from `shapes/work-order.shacl.ttl`; when admitted, this row and `sj:ledger-unsupported-002` retire | 2026-09-22
lib/ggen_igniter/semantic_jira/shacl.ex (`result_path/1`, `sh:targetSubjectsOf` repair, `compile_pattern/2` `dollar_endonly`), lib/ggen_igniter/semantic_jira/kernel_differential.ex (`has_predicate?/3` repair) | SHACL-SPARQL `?path` -> violation path (sh:resultPath), a working `sh:targetSubjectsOf` (`RDF.Description.include?/3` raised FunctionClauseError on a bare predicate under rdf 3.0.1), and XPath `fn:matches` `$` anchoring for `sh:pattern` (PCRE default `$` also matched before a final newline, so `"recipe:mix-format\n"` passed sj:CapabilityShape while the SA2A route `\z` refused it) | UNSUPPORTED(generator-capability): no admitted SHACL engine exists for this stack (hex.pm `shacl` 404, see the `GgenIgniter.SemanticJira.Shacl` moduledoc), so the court itself is hand-written residue | retire with the court: when an admitted SHACL engine (hex package or the NIF-backed validator) replaces `GgenIgniter.SemanticJira.Shacl`, these edits and this row retire | 2026-09-22
